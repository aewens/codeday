// Generated by CoffeeScript 1.7.1
(function() {
  define(["keys", "store"], function(Keys, Store) {
    return {
      isset: function(key) {
        return Keys.pressed[key];
      },
      commands: {
        newline: function(enter, edit) {
          if (!(enter === Keys.enter && !$("#dialog").is(":visible"))) {
            return;
          }
          return edit.line();
        },
        savefile: function(s, edit) {
          if (!(Keys.pressed[Keys.ctrl] && s === Keys.s)) {
            return;
          }
          e.preventDefault();
          return $("#dialog").show().focus().attr("placeholder", "File name...").on("keyup", function(e) {
            var store;
            if (e.keyCode === Keys.enter) {
              store = new Store($("#dialog"), $("textarea"), $("#files"), $("#info"), $("#save"));
              store.saveFile($("#dialog").val(), $("textarea").html());
              store.listFiles();
              return $(this).hide();
            } else if (e.keyCode === Keys.esc) {
              $(this).hide();
              return $("textarea").focus();
            }
          });
        }
      },
      run: function(key, edit) {
        var cmd, _results;
        _results = [];
        for (cmd in this.commands) {
          _results.push(this.commands[cmd](key, edit));
        }
        return _results;
      }
    };
  });

}).call(this);
